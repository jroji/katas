// Que crees que mostrará counter?
// Podrías cambiar el código de alguna manera para que muestre 5?
const createCounter = () => {
  const counter = 5;
  return counter;
}

createCounter();

// ❓
console.log(counter);
